
#' This is a report.
#' These are comments.
#' But, because I prefaced
#' them with "#'"
#' my comments will look like
#' report text.

x <- runif(1e3, 0, 10)

# However, this will look like 
# code comments.
str(x)
head(x)
summary(x)
hist(x)

x <- c('CA', 'WA', 'OR')



